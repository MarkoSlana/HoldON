<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:HoldON.ViewModels"
             xmlns:charts="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             x:DataType="viewmodels:ProgressViewModel"
             x:Class="HoldON.Views.ProgressPage"
             BackgroundColor="{StaticResource Background}">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">
            <Label Text="Napredek"
                   TextColor="{StaticResource White}"
                   FontSize="28"
                   FontAttributes="Bold"/>

            <!-- Stats Summary -->
            <Grid ColumnDefinitions="*, *" ColumnSpacing="15">
                <Frame Grid.Column="0" Style="{StaticResource StrongerCard}" Padding="20">
                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="📅" FontSize="20"/>
                            <Label Text="Dokončani treningi" TextColor="{StaticResource Gray400}" FontSize="12"/>
                        </HorizontalStackLayout>
                        <Label Text="18" TextColor="{StaticResource White}" FontSize="32" FontAttributes="Bold"/>
                        <Label Text="Ta mesec" TextColor="{StaticResource Gray400}" FontSize="12"/>
                    </VerticalStackLayout>
                </Frame>
                <Frame Grid.Column="1" Style="{StaticResource StrongerCard}" Padding="20">
                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="📈" FontSize="20"/>
                            <Label Text="Skupni volumen" TextColor="{StaticResource Gray400}" FontSize="12"/>
                        </HorizontalStackLayout>
                        <Label Text="37,500" TextColor="{StaticResource White}" FontSize="32" FontAttributes="Bold"/>
                        <Label Text="+12%" TextColor="{StaticResource Tertiary}" FontSize="12" FontAttributes="Bold"/>
                    </VerticalStackLayout>
                </Frame>
            </Grid>

            <!-- Navigation Tabs -->
            <Grid ColumnDefinitions="*, *, *" ColumnSpacing="0">
                <Button Grid.Column="0"
                        Text="Pregled"
                        Command="{Binding SelectViewCommand}"
                        CommandParameter="Pregled"
                        CornerRadius="25"
                        HeightRequest="40"
                        FontAttributes="Bold"
                        Padding="0">
                    <Button.Triggers>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="0">
                            <Setter Property="BackgroundColor" Value="{StaticResource White}"/>
                            <Setter Property="TextColor" Value="{StaticResource Background}"/>
                        </DataTrigger>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="1">
                            <Setter Property="BackgroundColor" Value="Transparent"/>
                            <Setter Property="TextColor" Value="{StaticResource Gray400}"/>
                        </DataTrigger>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="2">
                            <Setter Property="BackgroundColor" Value="Transparent"/>
                            <Setter Property="TextColor" Value="{StaticResource Gray400}"/>
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
                <Button Grid.Column="1"
                        Text="Vaje"
                        Command="{Binding SelectViewCommand}"
                        CommandParameter="Vaje"
                        CornerRadius="25"
                        HeightRequest="40"
                        FontAttributes="Bold"
                        Padding="0">
                    <Button.Triggers>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="0">
                            <Setter Property="BackgroundColor" Value="Transparent"/>
                            <Setter Property="TextColor" Value="{StaticResource Gray400}"/>
                        </DataTrigger>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="1">
                            <Setter Property="BackgroundColor" Value="{StaticResource White}"/>
                            <Setter Property="TextColor" Value="{StaticResource Background}"/>
                        </DataTrigger>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="2">
                            <Setter Property="BackgroundColor" Value="Transparent"/>
                            <Setter Property="TextColor" Value="{StaticResource Gray400}"/>
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
                <Button Grid.Column="2"
                        Text="Moč"
                        Command="{Binding SelectViewCommand}"
                        CommandParameter="Moč"
                        CornerRadius="25"
                        HeightRequest="40"
                        FontAttributes="Bold"
                        Padding="0">
                    <Button.Triggers>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="0">
                            <Setter Property="BackgroundColor" Value="Transparent"/>
                            <Setter Property="TextColor" Value="{StaticResource Gray400}"/>
                        </DataTrigger>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="1">
                            <Setter Property="BackgroundColor" Value="Transparent"/>
                            <Setter Property="TextColor" Value="{StaticResource Gray400}"/>
                        </DataTrigger>
                        <DataTrigger TargetType="Button" Binding="{Binding SelectedViewIndex}" Value="2">
                            <Setter Property="BackgroundColor" Value="{StaticResource White}"/>
                            <Setter Property="TextColor" Value="{StaticResource Background}"/>
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
            </Grid>

            <!-- Pregled View -->
            <VerticalStackLayout IsVisible="{Binding SelectedViewIndex, Converter={StaticResource EqualToConverter}, ConverterParameter=0}" Spacing="15">
                <Frame Style="{StaticResource StrongerCard}" Padding="20">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="Tedenski volumen"
                               TextColor="{StaticResource White}"
                               FontSize="18"
                               FontAttributes="Bold"/>
                        <charts:ChartView Chart="{Binding VolumeChart}" HeightRequest="250" BackgroundColor="Transparent" />
                    </VerticalStackLayout>
                </Frame>

                <Frame Style="{StaticResource StrongerCard}" Padding="20">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="Aktivnost"
                               TextColor="{StaticResource White}"
                               FontSize="18"
                               FontAttributes="Bold"/>
                        <Grid RowDefinitions="20,20,20,20,20,20,20"
                              ColumnDefinitions="20,20,20,20,20,20,20,20,20,20,20,20,20,20,20,20,20,20,20,20,20"
                              RowSpacing="4"
                              ColumnSpacing="4"
                              HorizontalOptions="Start">
                            <BoxView Grid.Row="6" Grid.Column="0" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="6" Grid.Column="1" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="6" Grid.Column="2" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="5" Grid.Column="3" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="4" Grid.Column="4" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="3" Grid.Column="4" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="2" Grid.Column="5" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="3" Grid.Column="5" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="4" Grid.Column="5" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="5" Grid.Column="5" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                            <BoxView Grid.Row="6" Grid.Column="5" Color="{StaticResource Tertiary}" CornerRadius="3"/>
                        </Grid>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>

            <!-- Vaje View -->
            <VerticalStackLayout IsVisible="{Binding SelectedViewIndex, Converter={StaticResource EqualToConverter}, ConverterParameter=1}" Spacing="15">
                <Frame Style="{StaticResource StrongerCard}" Padding="20">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="Napredek skozi čas"
                               TextColor="{StaticResource White}"
                               FontSize="18"
                               FontAttributes="Bold"/>

                        <!-- Exercise Selection -->
                        <HorizontalStackLayout Spacing="10">
                            <Button Text="Bench press"
                                    Command="{Binding SelectExerciseCommand}"
                                    CommandParameter="Bench press"
                                    BackgroundColor="{StaticResource Primary}"
                                    TextColor="{StaticResource White}"
                                    CornerRadius="20"
                                    HeightRequest="36"
                                    Padding="15,0"
                                    FontSize="12"/>
                            <Button Text="Počep"
                                    Command="{Binding SelectExerciseCommand}"
                                    CommandParameter="Počep"
                                    BackgroundColor="{StaticResource Gray600}"
                                    TextColor="{StaticResource Gray400}"
                                    CornerRadius="20"
                                    HeightRequest="36"
                                    Padding="15,0"
                                    FontSize="12"/>
                            <Button Text="Mrtvi dvig"
                                    Command="{Binding SelectExerciseCommand}"
                                    CommandParameter="Mrtvi dvig"
                                    BackgroundColor="{StaticResource Gray600}"
                                    TextColor="{StaticResource Gray400}"
                                    CornerRadius="20"
                                    HeightRequest="36"
                                    Padding="15,0"
                                    FontSize="12"/>
                        </HorizontalStackLayout>

                        <charts:ChartView Chart="{Binding ExerciseChart}" HeightRequest="250" BackgroundColor="Transparent" />
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>

            <!-- Moč View -->
            <VerticalStackLayout IsVisible="{Binding SelectedViewIndex, Converter={StaticResource EqualToConverter}, ConverterParameter=2}" Spacing="15">
                <Frame Style="{StaticResource StrongerCard}" Padding="20">
                    <VerticalStackLayout Spacing="20">
                        <Label Text="Standardi moči"
                               TextColor="{StaticResource White}"
                               FontSize="18"
                               FontAttributes="Bold"/>

                        <!-- Bench Press -->
                        <VerticalStackLayout Spacing="10">
                            <Grid ColumnDefinitions="*, Auto">
                                <Label Grid.Column="0" Text="Bench press" TextColor="{StaticResource White}" FontSize="16"/>
                                <Label Grid.Column="1" Text="Vaš PR: 95 kg" TextColor="{StaticResource Primary}" FontSize="14" FontAttributes="Bold"/>
                            </Grid>
                            <ProgressBar Progress="0.75" ProgressColor="{StaticResource Gold}" HeightRequest="12"/>
                            <Grid ColumnDefinitions="*, *, *, *">
                                <Label Grid.Column="0" Text="Začetnik" TextColor="{StaticResource Gray400}" FontSize="10"/>
                                <Label Grid.Column="1" Text="Vmesni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="Center"/>
                                <Label Grid.Column="2" Text="Napredni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="Center"/>
                                <Label Grid.Column="3" Text="Elitni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="End"/>
                            </Grid>
                        </VerticalStackLayout>

                        <!-- Počep -->
                        <VerticalStackLayout Spacing="10">
                            <Grid ColumnDefinitions="*, Auto">
                                <Label Grid.Column="0" Text="Počep" TextColor="{StaticResource White}" FontSize="16"/>
                                <Label Grid.Column="1" Text="Vaš PR: 120 kg" TextColor="{StaticResource Primary}" FontSize="14" FontAttributes="Bold"/>
                            </Grid>
                            <ProgressBar Progress="0.80" ProgressColor="{StaticResource Gold}" HeightRequest="12"/>
                            <Grid ColumnDefinitions="*, *, *, *">
                                <Label Grid.Column="0" Text="Začetnik" TextColor="{StaticResource Gray400}" FontSize="10"/>
                                <Label Grid.Column="1" Text="Vmesni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="Center"/>
                                <Label Grid.Column="2" Text="Napredni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="Center"/>
                                <Label Grid.Column="3" Text="Elitni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="End"/>
                            </Grid>
                        </VerticalStackLayout>

                        <!-- Mrtvi dvig -->
                        <VerticalStackLayout Spacing="10">
                            <Grid ColumnDefinitions="*, Auto">
                                <Label Grid.Column="0" Text="Mrtvi dvig" TextColor="{StaticResource White}" FontSize="16"/>
                                <Label Grid.Column="1" Text="Vaš PR: 140 kg" TextColor="{StaticResource Primary}" FontSize="14" FontAttributes="Bold"/>
                            </Grid>
                            <ProgressBar Progress="0.85" ProgressColor="{StaticResource Gold}" HeightRequest="12"/>
                            <Grid ColumnDefinitions="*, *, *, *">
                                <Label Grid.Column="0" Text="Začetnik" TextColor="{StaticResource Gray400}" FontSize="10"/>
                                <Label Grid.Column="1" Text="Vmesni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="Center"/>
                                <Label Grid.Column="2" Text="Napredni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="Center"/>
                                <Label Grid.Column="3" Text="Elitni" TextColor="{StaticResource Gray400}" FontSize="10" HorizontalTextAlignment="End"/>
                            </Grid>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
